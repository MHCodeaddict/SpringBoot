--Creates the Table password with some variables
CREATE TABLE password(
id LONG PRIMARY KEY,
title VARCHAR(20),
username VARCHAR(30),
password VARCHAR(30),
url VARCHAR(80),
email VARCHAR(80),
notes VARCHAR(255)
);
